node_modules/
dist/
.env
.env.local
keystore.json
keystore.json*
keystore.*.bak
# Foundry artifacts (root-anchored so they never match nested dirs like src/lib)
/out/
/cache/
/broadcast/
/lib/
.DS_Store
.claude/*
# Track the SessionStart hook + harness settings so Claude Code on the web can
# bootstrap a fresh container with Foundry + forge-std + the compiled artifact
# (otherwise 5 tests skip — see commit message for context), and so the calm
# MCP server config below actually reaches web sessions too.
!.claude/hooks/
!.claude/settings.json

# Autonomous-loop runtime dashboard output (T5.1 runner)
dashboard/
.codeindex/
.calm/

# Agentic session artifacts (local-only)
docs/superpowers/
docs/superskills/
.playwright-mcp/
