## What & why

Brief description of the change and the problem it solves.

## Checklist

- [ ] `pnpm typecheck` passes
- [ ] `pnpm test` passes
- [ ] `pnpm audit --audit-level high` is clean (or new finding is explained below)
- [ ] For `contracts-odra/` changes: `cargo +nightly test --manifest-path contracts-odra/Cargo.toml` passes
- [ ] Docs updated if this changes public behavior (README, `docs/standards/`, `DEMO_CASPER.md`/`DEMO_STELLAR.md`)

## Testing

How was this verified? Link to test output, a script run, or a live testnet transaction if applicable.
